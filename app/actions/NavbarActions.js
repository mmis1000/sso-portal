import alt from '../alt';

class FooterActions {
  constructor() {
    this.generateActions(
    );
  }

  /*updateLoginedUser(user) {
    this.actions.userUpdate(user)
  }*/
}

export default alt.createActions(FooterActions);